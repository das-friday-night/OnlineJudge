import { ProblemListFilterPipe } from './problem-list-filter.pipe';

describe('ProblemListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProblemListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
